# ROS2 parameters for cartographer_occupancy_grid_node
# Corresponds to byd_amr_lds.launch.py

cartographer_occupancy_grid_node:
  ros__parameters:
    # Use simulation time
    use_sim_time: true
    
    # Map resolution (meters per pixel)
    resolution: 0.05